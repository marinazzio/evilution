name: Lint and Test

on:
  push: {}

jobs:
  build:
    runs-on: ubuntu-latest
    name: Test on Ruby ${{ matrix.ruby }}
    strategy:
      matrix:
        ruby:
          - '3.3.11'
          - '3.4.9'
          - '4.0.2'

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          persist-credentials: false
      - name: Set up Ruby
        uses: ruby/setup-ruby@8e41b362d2589a22a44c1cfa214b3c83052c195b # v1.318.0
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler-cache: true
      - name: Run linter
        run: bundle exec rubocop

      - name: Run tests
        run: bundle exec rspec spec
        timeout-minutes: 5

  min_prism:
    runs-on: ubuntu-latest
    name: Resolve and test against minimum prism (1.5.x)
    env:
      BUNDLE_GEMFILE: ${{ github.workspace }}/Gemfile.min_prism
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v6
        with:
          persist-credentials: false
      - name: Set up Ruby
        uses: ruby/setup-ruby@8e41b362d2589a22a44c1cfa214b3c83052c195b # v1.318.0
        with:
          ruby-version: '3.3.11'
          bundler-cache: true
      - name: Verify prism resolved to 1.5.x
        run: |
          bundle exec ruby -rprism -e 'abort "prism is #{Prism::VERSION}, expected ~> 1.5.0" unless Gem::Requirement.new("~> 1.5.0").satisfied_by?(Gem::Version.new(Prism::VERSION))'
      - name: Run tests against minimum prism
        run: bundle exec rspec spec
        timeout-minutes: 5
